{
  "name": "plex-watchlist-cleaner-ui",
  "version": "0.0.1",
  "description": "UI to clean all the stuff inside plex watchlist",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production && npm run plex && node ./src/index.js",
		"dev": "nodemon ./src/index.js",
    "plex": "pip install --break-system-packages -r requirements.txt"
  },
  "keywords": ["plex", "watchlist"],
  "author": "wajeht",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
